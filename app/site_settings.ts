import { json } from "stream/consumers"
import json_settings from "../site_settings.json"

export const DEFAULT_TIME = json_settings.default_time
export const DEFAULT_LANG = json_settings.default_lang
export const APP_NAME = json_settings.app_name
